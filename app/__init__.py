# This file marks the `app` directory as a Python *package*.
#
# A package is just a directory that Python can import from. Without this
# `__init__.py` file, `from app.taskutils import parse_task` could fail with
# a ModuleNotFoundError depending on how Python is run.
#
# It can be completely empty — its mere existence is what matters. We keep it
# empty here because we have no package-level setup to do.
